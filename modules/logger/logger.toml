[module]
socket_file  = "/tmp/agent.pid"
timeout      = 1
files        = ["/var/log/messages", "/var/log/pacman.log", "/var/log"]

table_name   = "logs"

create_table = '''[
		            {"column_name" : "file",    "type": "TEXT"}, 
		            {"column_name" : "message", "type": "TEXT"}, 
		  ]'''
